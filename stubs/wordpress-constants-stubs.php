<?php
/**
 * Stub constants for WordPress that php-stubs/wordpress-stubs deliberately
 * omits (it excludes globals and constants).
 *
 * Loaded only by static analysis (Phan via `directory_list`, Psalm via
 * `<stubs>`) so the analyzers can resolve them without a WordPress install.
 * Deliberately free of an ABSPATH guard — analyzers load this outside a
 * WordPress request.
 *
 * @package ComicEaselREST
 */

define( 'ABSPATH', '/' );
define( 'DAY_IN_SECONDS', 86400 );
